{
    // Algorithm parameters.
    parameters: {
        par1: 10.3456789,
        par2: "abc def",
        par3: true
    },
    
    // Input data stack content. Items can be removed only from the stop of the 
    // stack to be consumed by Stages. Un-consumed items stay where ther are.
    // To remove the top N item(s), specify a name for the Stage to use 
    // internally and the object class for run-time validation.
    input: [
        ["image", "owl_pipe.TestModel.Image"],
        ["bpm", "owl_pipe.TestModel.Image"],
        ["variance", "owl_pipe.TestModel.Image"]
    ],
    
    // Output data stack content. Items can only be added to the stop of the 
    // stack (to be consumed by Stages further downstream). Existing items in
    // the stack stay where ther are.
    // To add N item(s) to the top of the stack, specify the object name as used
    // in the Stage internally and the object class for run-time validation.
    output: [
        ["processed_image", "owl_pipe.TestModel.Image"],
        ["processed_bpm", "owl_pipe.TestModel.Image"],
        ["processed_variance", "owl_pipe.TestModel.Image"]
    ]
}
